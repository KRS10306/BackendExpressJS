<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <style>
        html,body{
            height: 100%;
            width: 100%;
        }
    </style>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-8">
            <a href="/read" class="text-blue-500">Back</a>
        </div>
        <form action="/update/<%= user._id %>" method="post">
            <h1 class="text-4xl tracking-tighter"><strong>Update User</strong></h1>
            <hr class="mt-2 mb-4">
            <input type="text" name="name" class="px-5 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-lg" value="<%= user.name %>">
            <input type="email" name="email" class="px-5 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-lg" value="<%= user.email %>">
            <input type="text" name="image" class="px-5 py-2 outline-none bg-transparent border-2 border-zinc-800 rounded-lg" value="<%= user.image %>">
            <input type="submit" class="px-5 py-2 bg-yellow-500 rounded-lg ml-3" value="Update User">
        </form>
    </div>
</body>
</html>
